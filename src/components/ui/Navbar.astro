---
const navLinks = [
  { name: 'Inicio', href: '/' },
  { name: 'Nosotros', href: '/nosotros' },
  { name: 'Productos', href: '/productos' },
  { name: 'Contacto', href: '/contacto' },
];
---

<nav class="absolute top-0 left-0 w-full z-50 bg-linear-to-b from-black/60 to-transparent">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-24 items-center">
      
      <a href="/" class="flex items-center group">
        <div class="shrink-0 flex items-center gap-3">
          <div class="w-14 h-14 bg-white rounded-full flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform p-1">
             <div class="w-full h-full bg-brand-green rounded-full flex items-center justify-center text-white font-black text-2xl">L</div>
          </div>
          <span class="font-black text-white text-xl leading-none uppercase italic drop-shadow-md">
            Luyasa<br/><span class="text-xs tracking-widest not-italic font-bold text-brand-sun">Perú SAC</span>
          </span>
        </div>
      </a>
      
      <div class="hidden md:flex items-center space-x-8">
        {navLinks.map(link => (
          <a href={link.href} class="text-sm font-black text-white hover:text-brand-sun transition-colors uppercase tracking-widest drop-shadow-md">
            {link.name}
          </a>
        ))}
        <a href="/contacto" class="bg-brand-sun text-white px-8 py-3 rounded-full font-black text-sm hover:bg-white hover:text-brand-dark transition-all shadow-lg shadow-brand-sun/30 uppercase italic">
          Cotizar
        </a>
      </div>

      <div class="md:hidden flex items-center">
        <button class="text-white p-2">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 drop-shadow-md" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
          </svg>
        </button>
      </div>
    </div>
  </div>
</nav>